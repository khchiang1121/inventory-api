{{- if .Values.namespace.create }}
apiVersion: v1
kind: Namespace
metadata:
  name: {{ .Values.namespace.name | default (include "inventory-api.fullname" .) }}
  labels:
    {{- include "inventory-api.labels" . | nindent 4 }}
    name: {{ .Values.namespace.name | default (include "inventory-api.fullname" .) }}
{{- end }} 