apiVersion: v1
kind: Service
metadata:
  name: {{ include "virtflow-api.fullname" . }}
  labels:
    {{- include "virtflow-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "virtflow-api.selectorLabels" . | nindent 4 }} 